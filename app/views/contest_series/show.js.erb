if (window.history.pushState!=null) window.history.pushState(null, '', '/contest_series/<%=params[:id].to_s%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'contest_series/show')).html_safe %>");


