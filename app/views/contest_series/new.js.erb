if (window.history.pushState!=null) window.history.pushState(null, 'Huts on the Air', '/contest_series/new');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'contest_series/new')).html_safe %>");


