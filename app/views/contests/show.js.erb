if (window.history.pushState!=null) window.history.pushState(null, '', '/contests/<%=params[:id].to_s%>');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'contests/show')).html_safe %>");


