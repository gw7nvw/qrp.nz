if (window.history.pushState!=null) window.history.pushState(null, 'Huts on the Air', '/contests/new');
$('#right_panel').html("<%= escape_javascript(render(:partial => 'contests/new')).html_safe %>");


