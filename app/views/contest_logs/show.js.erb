if (window.history.pushState!=null) window.history.pushState(null, '', '/contest_logs/show');
$('#right_panel').html("<%= escape_javascript(render(:partial => '/contest_logs/show')).html_safe %>");


